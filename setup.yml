- hosts: localhost
  vars:
  pre_tasks:
  - name: update apt
    become: true
    apt:
      force_apt_get: true
      update_cache: true
      state: present
    tags:
      - install
      - core
  tasks:
    - include: tasks/git.yml
    - include: tasks/zsh.yml
      #- include: dotfiles.yml
      #- include: tasks/nvim.yml

